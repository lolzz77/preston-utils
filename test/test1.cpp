
unsigned int Test::m_test[ 128 ] = {0};

const uint32_t Manager::m_list[] = { 0x1, 0x2, 0x3 };

int main()
{
    int ret = main(a, b, c);

    if()
        int ret = main(a, b, c);

    if(VLIF == Class::Func())
    {
        int ret = main(a, b, c);
    }
    else
    {
        int ret = main(a, b, c);
    }

    switch(Class::Func())
    {
        case 1:
            int ret = main(a, b, c);
            break;
        case 2:
            int ret = main(a, b, c);
    int ret = main(a, b, c);
            int ret = main(a, b, c);
            break;
        case 3:int ret = main(a, b, c);
            break;
        default:
            break;
    }
}

inline int main()
{
    int ret = main(a, b, c);
}

inline
int main()
{
    int ret = main(a, b, c);
}

int main()
{
    int ret = main(a, b, c);
}

const int Class::Func[] = {

    int ret = main(a, b, c);
};

const int Class::Func[] = {
    int ret = main(a, b, c);

};