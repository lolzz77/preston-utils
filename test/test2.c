

unsigned int _test[ 128 ] = {0};

const uint32_t _list[] = { 0x1, 0x2, 0x3 };

inline int main()
{
    int a;
    float b;
    int ret = main(a, b, c);
}

void main()
{
    main();
#ifdef A
    int x = main();
    if ()
    {
    }
    else
    {
        main();
    }
#endif
    main();


#if defined A
    main();
    main();
#endif
    main();

    int ret = main(a, b, c);

    // for this case, should not put log in between 'if' 'else'
    // like, after the ending bracket '}' of the 'if'
    // it will cause compilation error 'expected a statement'
    if()
    {
        int ret = main(a, b, c);
    }
    else{
        int ret = main(a, b, c);
    }

    if(
        test() == TRUE
    )
    {
        main();
    }

    if(
        test() == true &&
    )
    {
        main();
    }

    if(
        A==true &&
        test() == true &&
        B==true
    )
    {
        main();
    }

    main(
        a,
        b,
        c
    );

    if(
        A==true &&
        main(
            a,
            b,
            c
        ) &&
        B==true
    )
    {

    }

    return main(a,b,c);

    return (main(a,b,c))

    return (
        A,
        main(a,b,c),
        B
    )

    if(A==true, main(a,b,c), B==true) {}

    if()
        int ret = main(a, b, c);

    if()
        ret = main(a, b, c);

    if()
        main(a, b, c);

    if()
    {
        int ret = main(a, b, c);

    }
    else
    {
        int ret = (int*)main(a, b, c);
        main(d, e, f);
        (float*)main(d, e, f);

    }
    switch (main(a, b, c))
    {
        int ret = main(a, b, c);

        if()
        {
            int ret = main(a, b, c);
        }

    case 1:
        int ret = main(a, b, c);
        int ret = main(a, b, c);
        int ret = main(a, b, c);
        break;

    case 2:
        int ret = main(a, b, c);
int ret = main(a, b, c);
        int ret = main(a, b, c);
        break;

    case 3:int ret = main(a, b, c);
        break;
    
    default:
        break;
    }
    
}

inline int main()
{
    int ret = main(a, b, c);
}

inline
int main()
{
    int ret = main(a, b, c);
    
}

int main()
{
    int ret = main(a, b, c);
}

void func(int fd){int ret = main(a, b, c);}

void func(int x{});

void func(int x{}){int ret = main(a, b, c);}

int main() { int x; int ret = main(a, b, c);}